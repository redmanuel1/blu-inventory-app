export interface User {
    idNo: string;
    password: string;
    role: string;
    firstName: string,
    lastName: string,
    email: string,
    phone: string,
    // Add other properties as needed
  }